# Fork of the [React Example App](https://github.com/ahfarmer/emoji-search)

Just a simple app with CI/CD

## Install
`npm install`

## Usage
`npm start`

## Docker
Or in one command
`docker-compose up --build -d`